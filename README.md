The master is working with the LLM API, but without access to a website that can't be searched online.
The agent_branch is working using the Agent API, but it is not always working well.
We need to decide which way to continue.
